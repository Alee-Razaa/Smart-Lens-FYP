# Smart Lens - FYP Project 🔍

## Setup

### Google Colab (GPU Training)
1. Open `colab_setup.ipynb` in Google Colab
2. Follow the instructions to connect VS Code via SSH

### Local Development
1. Clone this repo: `git clone <your-repo-url>`
2. Install dependencies: `pip install -r requirements.txt`

## Project Structure
```
├── colab_setup.ipynb    # Colab GPU + SSH setup notebook
├── Labeled data api.txt # Labeled data API reference
├── .gitignore
└── README.md
```
